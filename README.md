# demo for use @tanstack in reactjs and angular zoneless

## build and run

This workspace uses pnpm as the package manager. You can install it globally using npm:

```bash
# pnpm install
corepack enable

# run angular app
pnpm nx serve angular-to-do


# run react app
pnpm nx serve react-to-do
```
